int bubble(int qtd)
int i;
int j;
int aux;
int k;
{
  k = qtd - 1;

  i=0;
  while (i < qtd) do
  {
    j=0;
    while (j < k) do
     {
        if (vector2order[j] > vector2order[j+1]) then
        {
          aux = vector2order[j];
          vector2order[j] = vector2order[j+1];
          vector2order[j+1] = aux;
        };
        j=j+1
     };
     k=k-1;
     i=i+1
  };
  
  return vector2order
}
